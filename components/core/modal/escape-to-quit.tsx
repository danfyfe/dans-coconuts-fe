import P from "../typography/P";

const EscapeToQuitDisclaimer = () => (
  <P className="text-xs !text-right mt-2">* press the escape key at any time to quit.</P>
);

export default EscapeToQuitDisclaimer;
