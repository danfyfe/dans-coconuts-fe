import P from "../typography/P";

const EscapeToQuitDisclaimer = () => (
  <P className="text-xs !text-right mt-2 hidden md:block">* press the escape key at any time to quit the tour.</P>
);

export default EscapeToQuitDisclaimer;
