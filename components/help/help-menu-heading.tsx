import H2 from "../core/typography/H2";

const HelpMenuHeading = ({ heading }: { heading: string }) => {
  return (
    <H2 className="text-xl">{heading} Help (WIP)</H2>
  )
};

export default HelpMenuHeading;
